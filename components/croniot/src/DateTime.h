#ifndef DATETIME_H
#define DATETIME_H

//TODO include date and zone in the future
class DateTime {

    public:
        DateTime() : hour(0), minute(0), second(0) {}

        int hour;
        int minute;
        int second;

    private:

};

#endif